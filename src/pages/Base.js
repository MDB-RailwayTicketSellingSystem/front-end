import React, { Component } from 'react';

import NavBar from '../component/NavBar'

class Base extends Component {
    render() {
        return (
            <div className="Base">

                <NavBar />
            </div>

        );
    }
}

export default Base;
